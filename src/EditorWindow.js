import React from 'react';

const EditorWindow = (props) => {
    return (

        <textarea 
        className="form-control editor-window" 
        rows="3" 
        value="it was the best of times, it was the pretty okish of times."
        />

    );
};

export default EditorWindow;